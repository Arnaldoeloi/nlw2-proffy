import React from 'react';

//you don't need to reference index file because the project will look for it by default

import './assets/styles/global.css';
import Routes from './pages/routes';

//JSX = Javascript + XML

function App() {
  return (
    <Routes />
  );
}

export default App;
